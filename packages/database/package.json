{
  "name": "@repo/db",
  "version": "0.0.0",
  "devDependencies": {
    "@faker-js/faker": "^8.4.1",
    "@types/node": "^22.5.1",
    "prisma": "^5.22.0",
    "tsx": "^4.17.0"
  },
  "scripts": {
    "db:start": "docker-compose -f docker-compose-database.yaml up -d",
    "db:generate": "prisma generate",
    "db:migrate": "prisma db push --skip-generate",
    "db:seed": "tsx ./src/seed.ts"
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0"
  }
}